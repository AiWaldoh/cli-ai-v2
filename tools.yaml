tools:
  - type: function
    function:
      name: execute_command
      description: Runs a Linux command
      parameters:
        type: object
        properties:
          command:
            type: string
            description: The command from user input to execute.
        required:
          - command